CENSUS_API_KEY=your_census_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Optional: default OpenRouter model (must be one of the ids in src/llm.py MODELS).
# Leave unset to use the first registry entry (anthropic/claude-sonnet-5).
OPENROUTER_MODEL=
MAX_CONCURRENCY=10
